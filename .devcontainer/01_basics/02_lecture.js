"use strict"; //syntax so that engine treat all js code as newer version 
// alert("Nishant")// We are using node js not browser
/* Data Types: 
    number 
    bigint
    string
    boolean
    null: standalone value
    undefined
    symbol : for uniqueness
    object*/
    console.log(typeof null)